import { render, screen } from "@testing-library/react";
import { GifSearchApp } from "../src/GifSearchApp";

// TODO: Make component test
describe("Test over <GifSearchApp />", () => {
  test("Should ", () => {
    render(<GifSearchApp />);
    screen.debug();
  });
});
